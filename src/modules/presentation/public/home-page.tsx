import { Button } from "@/components/ui/button";

const HomePage = () => {
  return (
    <div>
      <h2 className="text-primary text-2xl font-semibold">Home Page</h2>
      <Button size="default">Welcome Lucia</Button>
    </div>
  );
};

export default HomePage;
